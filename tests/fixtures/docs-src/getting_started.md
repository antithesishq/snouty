# Setup guide

This page walks through a small local setup.

Use Docker Compose to start the sample app and supporting services.
The setup is intentionally simple so search results are easy to inspect.
