# Docker basics

Docker lets you package services in repeatable images.
A docker compose file is useful for local test environments.
You can rebuild the stack quickly when a test run needs a clean slate.
